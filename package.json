{
  "devDependencies": {
    "@types/node": "^18.11.18",
    "gts": "^3.1.1",
    "typescript": "^4.9.4",
    "yarn-upgrade-all": "^0.7.1"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  }
}
